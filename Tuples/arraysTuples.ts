const tuples: [string, number, boolean] = ["teste", 7, true];

const readonlyTuple: readonly [string, number, boolean] = ["teste", 3, true];
/**
 * it's a good practice create a tuples readonly. because you have more security in your tuple.
 * making your tuple readonly, is safety because anyone can't push whatever he want in 3+ index.
 */