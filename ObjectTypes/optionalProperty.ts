const car3: {type: string, year?: number} = {
    type: "Hyundai"
};

// car3.year = "2011"; optional.

/**
 * when you use "?" in one variable, you define this to a optional variable.
 * so... you don't need to declare a value to this variable.
 */